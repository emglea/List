import { Injectable } from '@angular/core';
import { List } from './list.model';

// dependency injection
// services are for managing, manipulating, storing data
@Injectable()
export class ListService {
  // inside the parentheses: private xService: XService, same for routes
  constructor() {}
}
